<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE struts PUBLIC
	"-//Apache Software Foundation//DTD Struts Configuration 2.3//EN"
	"http://struts.apache.org/dtds/struts-2.3.dtd">

<struts>
    <!-- 指定每次配置文件更改后，自动重新加载 -->
    <constant name="struts.configuration.xml.reload" value="true"/>
    <!-- 将 devMode设置为true后  修改本xml  不用在再重新启动服务器-->
	<constant name="struts.devMode" value="true" />
	<!-- 通过这个配置可以解决中文乱码问题  -->
	<constant name="struts.i18n.encode" value="GBK"/>
	<!-- 想要使用动态调用方法 必须将 DynamicMethodInvocation设置为true-->
    <constant name="struts.enable.DynamicMethodInvocation" value="true" />
    <!-- 想让页面访问静态方法，必须将这个设置为true -->
    <constant name="struts.ognl.allowStaticMethodAccess" value="true" />
    
    <package name="ognl" namespace="/ognl" extends="struts-default">
    	
    	<!-- 通配符 2 虽然配置简单 量少  但是新建类  jsp必须按相应的规则创建-->
    	<action name="ognl" class="com.soft.action.OgnlAction">
    		<result name="success">/ognl_view.jsp</result>
    	</action>
    </package>
        <package name="tag" namespace="/tags" extends="struts-default">
    	
    	<!-- 通配符 2 虽然配置简单 量少  但是新建类  jsp必须按相应的规则创建-->
    	<action name="tags" class="com.soft.action.TagsAction">
    		<result name="success">/tags_view.jsp</result>
    	</action>
    </package>
</struts>
